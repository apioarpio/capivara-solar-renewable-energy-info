export interface AppDictionaryInterface {
  chartIrradianceTitle: string;
  getCurrentPosition: string;
  getOtherPosition: string;
  gettingPosition: string;
  buildingChart: string;
  cloudAmount: string;
}
